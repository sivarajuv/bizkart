export const API_BASE_URL = '/api';

export const DEFAULT_LANGUAGE = 'en';
export const DEFAULT_BUSINESS_TYPE = 'Kirana Store';
export const DEFAULT_PLATFORM_LABEL = 'Platform';
export const DEFAULT_PLATFORM_SUBTITLE = 'Multi-business retail OS';
export const DEFAULT_EMPTY_STATE = '—';

export const PASSWORD_MIN_LENGTH = 8;

export const USER_ROLES = {
  SUPER_ADMIN: 'SUPER_ADMIN',
  ADMIN: 'ADMIN',
  MANAGER: 'MANAGER',
  CASHIER: 'CASHIER',
};

export const MANAGEABLE_USER_ROLE_OPTIONS = [
  USER_ROLES.ADMIN,
  USER_ROLES.MANAGER,
  USER_ROLES.CASHIER,
];
